<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.jesse.persistence.MemberMapper">

	<select id="getTime" resultType="String">
		select sysdate from dual
	</select>

	<!-- <insert id="create"> insert into tbl_member (usr, upw, uname, email, 
		phone, birth, gender) values (#{usr},#{upw},#{uname},#{email},#{phone},#{birth},#{gender}) 
		</insert> <select id="read" resultType="org.jesse.domain.MemberVO"> select 
		* from tbl_member where usr = #{usr} </select> <delete id="delete"> delete 
		from tbl_member where usr = #{usr} </delete> <update id="update"> update 
		tbl_member set upw=#{upw}, uname =#{uname}, email=#{email}, phone=#{phone}, 
		birth=#{birth}, gender =#{gender}, updateddate=sysdate where usr = #{usr} 
		</update> -->

	<!-- <select id="listAll" resultType="org.jesse.domain.MemberVO"> select 
		* from tbl_member </select> <select id="listPage" resultType="org.jesse.domain.MemberVO"> 
		select * from <![CDATA[(select /*+index_desc(tbl_member pk_member)*/ rownum 
		rn,mno,usr,upw,uname,email,phone,birth,gender,regdate,updateddate from tbl_member 
		where rownum <= #{page}*10 and mno>0) where rn>(#{page}-1)*10]]> </select> -->


</mapper>